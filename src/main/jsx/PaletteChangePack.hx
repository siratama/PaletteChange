package jsx;

import jsx.color_picker.PixelColorSearch;
import jsx.color_picker.PixelSelector;
import jsx.palette_change.PaletteChange;
import jsx.color_sampler.CanvasColorSampler;

class PaletteChangePack
{
	public static function main()
	{
		/*
		new CanvasColorSampler();
		new PaletteChange();
		new PixelSelector();
		new PixelColorSearch();
		*/
	}
	public function new()
	{
	}
}
